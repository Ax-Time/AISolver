package main;

public class Main {

    public static void main(String[] args) {
        /* Tic-Tac-Toe Test
        State startingState = new TicTacToeState();
        System.out.println(startingState + "\n");
        while(!startingState.isTerminal()){
            System.out.println(startingState);
            Action bestAction = MiniMax_Solver.getBestAction(startingState);
            startingState = startingState.applyAction(bestAction);
            System.out.println("Choose your move: ");
            Scanner in = new Scanner(System.in);
            int row = in.nextInt();
            int col = in.nextInt();
            startingState = startingState.applyAction(new TicTacToeAction(row, col));
        }

         */


    }
}
